import React from "react";

// Define formatting components
export const Bold = ({ children }) => <strong>{children}</strong>;
export const Italic = ({ children }) => <em>{children}</em>;
export const Underline = ({ children }) => <u>{children}</u>;
